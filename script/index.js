import { flipColors, multiplyNode } from "./function.js";

multiplyNode(document.querySelector('.pad'), 8);
multiplyNode(document.querySelector('.pad-group'), 6);

flipColors(["transparent", "rgb(0, 80, 80)"], ["0", "1em"]);
